% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_tf.R
\name{check_tf}
\alias{check_tf}
\title{Check Transcription Factor}
\usage{
check_tf(TF)
}
\arguments{
\item{TF}{A string giving the name of the transcription factor to be queried.}
}

\value{
The requested transcription-factor information, or \code{NULL} if it is unavailable.
}
\description{
This function leverages the information collected with TF_enrich_network()
to allow the user to query specific transcription factors of interest and see how their top targets are spread across modules.
The goal is to uncover potential co-regulations between clusters.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(check_tf("STAT1"), silent = TRUE)
}
}

