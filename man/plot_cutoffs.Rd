% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutoff_plot.R
\name{plot_cutoffs}
\alias{plot_cutoffs}
\title{Plot of Cutoff Statistics}
\usage{
plot_cutoffs(
  interactive = TRUE,
  hline = list(R.squared = NULL, no_edges = NULL, no_nodes = NULL, no_networks = NULL)
)
}
\arguments{
\item{interactive}{Boolean. If TRUE (default) plot is created using plotly including and interactive cutoff slider. If FALSE, plot is created as a static ggplot.}

\item{hline}{A list with four slots ("R.squared", "no_edges", "no_nodes", "no_networks") each of which can be set either to NULL (default) or a number to introduce a horizontal line for orientation at that value in the respective plot. Only used in the non-interactive plot.}
}

\value{
Renders cutoff-diagnostic plots and stores them in the legacy \code{hcobject}.
}
\description{
Plots the R-squared value, number of edges, number of genes and number of networks for different cut-offs.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(plot_cutoffs(interactive = FALSE), silent = TRUE)
}
}

