% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_clustering.R
\name{export_clusters}
\alias{export_clusters}
\title{Export Clustering}
\usage{
export_clusters()
}

\value{
Writes the cluster export to disk and invisibly updates the legacy \code{hcobject}.
}
\description{
Exports your clustering to a text file that can be imported into other hCoCena runs.
Format: Two columns, the first one named gene (containing the gene names) and the second one named color (containing the corresponding cluster color to which the gene belonged).
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(export_clusters(), silent = TRUE)
}
}

