% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcription_factor_enrichment.R
\name{TF_overrep_network}
\alias{TF_overrep_network}
\title{Network-Wide Transcription Factor Over-representation Analysis}
\usage{
TF_overrep_network(topTF = 100, topTarget = 30)
}
\arguments{
\item{topTF}{The number of transcription factors with the highest number of enriched targets in the network. Default is 100.}

\item{topTarget}{Per transcription factor the number of top most enriched targets to return. Default is 30.}
}

\value{
Returns network-level transcription-factor over-representation results.
}
\description{
Returns the transcription factors that have the most enriched targets network-wide, including their targets.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(TF_overrep_network(topTF = 5, topTarget = 3), silent = TRUE)
}
}

