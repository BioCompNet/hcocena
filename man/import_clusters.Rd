% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_clusters.R
\name{import_clusters}
\alias{import_clusters}
\title{Import Clusters From File}
\usage{
import_clusters(file, sep = "\\t", header = TRUE)
}
\arguments{
\item{file}{File path.}

\item{sep}{The separator of the file. Default is tab-separated.}

\item{header}{A Boolean. Whether or not the file has headers (column names).}
}

\value{
Returns the imported clustering and updates the legacy \code{hcobject}.
}
\description{
Uses an imported clustering model instead of clustering the integrated network.
The model must be saved as two columns, the first containing genes, the second cluster colors (this is the format export_clusters() exports to).
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
tmp_clusters <- tempfile(fileext = ".tsv")
utils::write.table(GeneToCluster(), file = tmp_clusters, sep = "\t", row.names = FALSE, quote = FALSE)
try(import_clusters(tmp_clusters), silent = TRUE)
}
}

