% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colour_single_cluster.R
\name{colour_single_cluster}
\alias{colour_single_cluster}
\title{Colour Single Cluster}
\usage{
colour_single_cluster(cluster)
}
\arguments{
\item{cluster}{A string, giving the colour of the cluster to be highlighted.}
}

\value{
Invisibly updates cached network plots in the legacy \code{hcobject}.
}
\description{
Replots the integrated network, highlighting a given cluster.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
mods <- unique(as.character(hcobject[["integrated_output"]][["cluster_calc"]][["cluster_information"]][["color"]]))
mods <- mods[!is.na(mods) & mods != "white"]
if (length(mods) > 0) try(colour_single_cluster(cluster = mods[1]), silent = TRUE)
}
}

