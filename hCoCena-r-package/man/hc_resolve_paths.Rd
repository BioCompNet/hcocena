% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_resolve_paths}
\alias{hc_resolve_paths}
\title{Resolve hCoCena input/output directories for Docker or local projects}
\usage{
hc_resolve_paths(
  project_root_hint = NULL,
  local_count_subdir = "count_data",
  local_annotation_subdir = "annotation_data",
  local_reference_subdir = "reference_files",
  local_output_subdir = "output",
  docker_reference_dir = "/home/rstudio/reference_files",
  docker_count_dir = "/home/rstudio/count_data",
  docker_annotation_dir = "/home/rstudio/annotation_data",
  docker_output_dir = "/home/rstudio/output",
  create_docker_dirs = TRUE,
  fallback_count_data = "PATH_TO_FOLDER_THAT_HOLDS_YOUR_GENE_EXPRESSION_TABLES/",
  fallback_annotation = "PATH_TO_FOLDER_THAT_HOLDS_YOUR_ANNOTATION_TABLES/",
  fallback_reference_files = "PATH_TO_REFERENCE_FILES_FOLDER/",
  fallback_output = "PATH_TO_FOLDER_WHERE_HCOCENA_SHOULD_SAVE_ALL_ANALYSIS_OUTPUTS/"
)
}
\arguments{
\item{project_root_hint}{Optional local project root to prioritize if it exists.
If missing or not found, the current working directory is used.}

\item{local_count_subdir}{Local count-data subfolder below \code{project_root_hint}.}

\item{local_annotation_subdir}{Local annotation-data subfolder below \code{project_root_hint}.}

\item{local_reference_subdir}{Local reference-files subfolder below \code{project_root_hint}.}

\item{local_output_subdir}{Local output subfolder below \code{project_root_hint}.}

\item{docker_reference_dir}{Docker reference-files directory used to detect Docker mode.}

\item{docker_count_dir}{Docker count-data directory.}

\item{docker_annotation_dir}{Docker annotation-data directory.}

\item{docker_output_dir}{Docker output directory.}

\item{create_docker_dirs}{Boolean. If TRUE and Docker mode is detected, create the
Docker directories when missing.}

\item{fallback_count_data}{Fallback count-data directory string used outside Docker
when no local folder was detected.}

\item{fallback_annotation}{Fallback annotation directory string used outside Docker
when no local folder was detected.}

\item{fallback_reference_files}{Fallback reference-files directory string used outside
Docker when no local folder was detected.}

\item{fallback_output}{Fallback output directory string used outside Docker when no
local folder was detected.}
}
\value{
Named list with \code{docker_mode}, \code{project_root}, \code{dir_count_data},
\code{dir_annotation}, \code{dir_reference_files}, and \code{dir_output}.
}
\description{
Resolve hCoCena input/output directories for Docker or local projects
}
