% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alluvial_plots.R
\name{algo_alluvial}
\alias{algo_alluvial}
\title{Plot Alluvial Plots For Different Clustering Algorithms}
\usage{
algo_alluvial()
}

\value{
Returns the alluvial plot object and stores the legacy output in \code{hcobject}.
}
\description{
Alluvial plots will be generated demonstrating how the genes change clusters if the current clustering algorithm (always shown on the left) was changed to any of the other clustering options (always shown on the right).
The produced plot is interactive, hover over it to get more details.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(algo_alluvial(), silent = TRUE)
}
}

