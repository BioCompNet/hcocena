% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_save_folder.R
\name{init_save_folder}
\alias{init_save_folder}
\title{Creates a Save Folder}
\usage{
init_save_folder(name)
}
\arguments{
\item{name}{The name of the folder to be created.
Use \code{""} to write outputs directly into \code{dir_output} without creating a subfolder.}
}

\value{
Invisibly updates the configured save folder in the legacy \code{hcobject}.
}
\description{
A folder with the given name is created in the output directory. All analysis outputs will be saved to this folder.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(init_save_folder(name = ""), silent = TRUE)
}
}

