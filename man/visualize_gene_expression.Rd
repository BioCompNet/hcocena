% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_gene_expression.R
\name{visualize_gene_expression}
\alias{visualize_gene_expression}
\title{Visualize Gene Expression}
\usage{
visualize_gene_expression(
  genes,
  name = NULL,
  width = 15,
  height = 10,
  save = TRUE
)
}
\arguments{
\item{genes}{A vector of strings giving the genes to be plotted.}

\item{name}{A string giving the plot title and the name for the save file (WITHOUT file ending).}

\item{width}{Width of the PDF, default ist 15, only change if plots overlap in PDF.}

\item{height}{Height of the PDF, default ist 10, only change if plots overlap in PDF.}

\item{save}{Bolean to determine whether the plot should be saved as PDF. TRUE by default.}
}

\value{
Returns the generated expression heatmap object.
}
\description{
Plots the mean expression values per condition for the given genes for each dataset as a heatmap. Values are scaled across rows.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
genes <- trimws(strsplit(as.character(hcobject[["integrated_output"]][["cluster_calc"]][["cluster_information"]][["gene_n"]][[1]]), ",", fixed = TRUE)[[1]])
genes <- genes[genes != ""]
if (length(genes) > 0) try(visualize_gene_expression(genes = genes[seq_len(min(3, length(genes)))], save = FALSE), silent = TRUE)
}
}

