% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highlight_geneset.R
\name{highlight_geneset}
\alias{highlight_geneset}
\title{Highlight Geneset}
\usage{
highlight_geneset(
  gene_set,
  name = NULL,
  col = "black",
  label_offset = 3,
  plot = TRUE,
  save = TRUE
)
}
\arguments{
\item{gene_set}{A vector of gene symbols (as strings) to be highlighted in the network.}

\item{name}{Optional legacy label argument retained for backward compatibility.}

\item{col}{A string specifying the color with which the nodes of the genes are framed to highlight them. Default is "black".}

\item{label_offset}{A numeric specifying the distance between the labeled genes and the network. Default is 3.}

\item{plot}{A Boolean. Wheather or not to plot the network (per cluster) with highlighted hub nodes. Default is FALSE.}

\item{save}{A Boolean. Whether or not a labelled hub-network per cluster and the expression heatmap are to be save to PDF. Default is FALSE.}
}

\value{
Invisibly updates cached network plots in the legacy \code{hcobject}.
}
\description{
Receives and highlights a list of genes in the integrated network.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
genes <- trimws(strsplit(as.character(hcobject[["integrated_output"]][["cluster_calc"]][["cluster_information"]][["gene_n"]][[1]]), ",", fixed = TRUE)[[1]])
genes <- genes[genes != ""]
if (length(genes) > 0) try(highlight_geneset(gene_set = genes[seq_len(min(3, length(genes)))], plot = FALSE, save = FALSE), silent = TRUE)
}
}

