% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_tf_overrep_network}
\alias{hc_tf_overrep_network}
\title{TF enrichment network-wide (S4 API)}
\usage{
hc_tf_overrep_network(hc, ...)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{...}{Passed to \code{TF_overrep_network()}.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
TF enrichment network-wide (S4 API)
}

\examples{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
is(hc, "HCoCenaExperiment")

\donttest{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
hco <- as_hcobject(hc)
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hc <- as_hcocena(hco)
try(hc <- hc_tf_overrep_network(hc, topTF = 5, topTarget = 3), silent = TRUE)
}
}

