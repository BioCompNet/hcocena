% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cluster_scores.R
\name{get_module_scores}
\alias{get_module_scores}
\title{Get Cluster Scores}
\usage{
get_module_scores(save = TRUE)
}
\arguments{
\item{save}{A Boolean. Whether or not to save the plot to PDF (default is TRUE).}
}

\value{
Returns the module-score data and stores accompanying legacy outputs in \code{hcobject}.
}
\description{
For every gene, the ratio of its edges to genes in the same cluster to its total number of edges is determined.
The corresponding values are returned as a data frame and a box plot is generated showing the scores for each of the clusters.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(get_module_scores(save = FALSE), silent = TRUE)
}
}

