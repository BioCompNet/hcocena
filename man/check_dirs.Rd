% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_dirs.R
\name{check_dirs}
\alias{check_dirs}
\title{Fixes Directories}
\usage{
check_dirs(create_output_dir = FALSE)
}
\arguments{
\item{create_output_dir}{Boolean. If TRUE and \code{dir_output} does not exist, it will be created.}
}

\value{
Invisibly updates normalized directory paths in the legacy \code{hcobject}.
}
\description{
Iteratively calls fix_dir() on the provided working directory paths to fix them if necessary or throw an error if they are invalid.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(check_dirs(create_output_dir = TRUE), silent = TRUE)
}
}

