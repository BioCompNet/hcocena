% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_topvar.R
\name{suggest_topvar}
\alias{suggest_topvar}
\title{Suggest Top Most Variant Genes}
\usage{
suggest_topvar()
}

\value{
Returns suggested top-variance cutoffs and stores legacy outputs in \code{hcobject}.
}
\description{
For each dataset, calculate the inflection points of the logged variance of gene expression values.
Returned values may be used for "topvar" in set_layer_settings().
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_prepared.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(suggest_topvar(), silent = TRUE)
}
}

