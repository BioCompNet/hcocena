% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_unsplit_modules}
\alias{hc_unsplit_modules}
\title{Undo module splitting (S4 API)}
\usage{
hc_unsplit_modules(hc, which = c("last", "all"), verbose = TRUE)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{which}{Either \code{"last"} (undo one split step) or \code{"all"} (restore the
original pre-split cluster state).}

\item{verbose}{Logical; print progress messages.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Restores module structure from split history.
}

\examples{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
is(hc, "HCoCenaExperiment")

\donttest{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
hco <- as_hcobject(hc)
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hc <- as_hcocena(hco)
try(hc <- hc_unsplit_modules(hc), silent = TRUE)
}
}

