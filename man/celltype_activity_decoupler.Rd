% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celltype_activity_decoupler.R
\name{celltype_activity_decoupler}
\alias{celltype_activity_decoupler}
\title{decoupleR-based module cell-type activity from Enrichr marker libraries}
\usage{
celltype_activity_decoupler(
  databases = c("Descartes_Cell_Types_and_Tissue_2021", "Human_Gene_Atlas"),
  custom_gmt_files = NULL,
  clusters = c("all"),
  mode = c("coarse", "fine"),
  top = 3,
  qval = 0.1,
  padj = "BH",
  activity_input = c("gfc", "fc", "expression"),
  fc_comparisons = NULL,
  method = "ulm",
  minsize = 5,
  min_term_genes = 5,
  annotation_slot = c("auto", "enriched_per_cluster", "enriched_per_cluster2"),
  slot_suffix = "decoupler",
  clear_previous_slots = FALSE,
  coarse_map = NULL,
  coarse_include_other = TRUE,
  refresh_db = FALSE,
  export_excel = TRUE,
  excel_file = "Module_Celltype_Activity_decoupler.xlsx",
  plot_heatmap = FALSE,
  heatmap_file_name = "Heatmap_modules_celltype_activity_decoupler.pdf",
  ...
)
}
\arguments{
\item{databases}{Character vector of Enrichr library names.}

\item{custom_gmt_files}{Optional custom GMT file(s) to include as additional
marker resources. Accepts a character vector or named list of file paths.}

\item{clusters}{Either \code{"all"} (default) or a character vector of module IDs/colors.}

\item{mode}{Either \code{"coarse"} (default) or \code{"fine"}.}

\item{top}{Number of selected marker activities per module.}

\item{qval}{Adjusted p-value cutoff.}

\item{padj}{Multiple-testing correction method.}

\item{activity_input}{One of \code{"gfc"} (default), \code{"fc"}, \code{"expression"}.}

\item{fc_comparisons}{Vector like \code{c("A_vs_B", "C_vs_B")} when \code{activity_input = "fc"}.}

\item{method}{decoupleR method suffix (e.g. \code{"ulm"}).}

\item{minsize}{Minimum target size passed to decoupleR.}

\item{min_term_genes}{Minimum genes per Enrichr term.}

\item{annotation_slot}{Legacy slot override for single-database runs.}

\item{slot_suffix}{Optional slot suffix; default \code{"decoupler"}.}

\item{clear_previous_slots}{If TRUE, clears old \verb{enriched_per_cluster*} slots first.}

\item{coarse_map}{Optional named regex map used in coarse mode.}

\item{coarse_include_other}{Keep \code{"Other"} class in coarse mode.}

\item{refresh_db}{Refresh Enrichr cache.}

\item{export_excel}{Write summary workbook.}

\item{excel_file}{Excel filename.}

\item{plot_heatmap}{Replot cluster heatmap with dynamic slots.}

\item{heatmap_file_name}{Heatmap filename when \code{plot_heatmap = TRUE}.}

\item{...}{Passed to \code{plot_cluster_heatmap()}.}
}
\value{
Invisibly returns a list with activity tables and slot mapping.
}
\description{
Computes module-wise activity scores for Enrichr-derived cell-type marker sets
using \code{decoupleR}, stores selected/significant results, and writes stacked-bar
annotation slots (\verb{enriched_per_cluster_*}) that can be rendered via
\code{plot_cluster_heatmap(include_dynamic_enrichment_slots = TRUE)}.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
genes <- trimws(strsplit(as.character(hcobject[["integrated_output"]][["cluster_calc"]][["cluster_information"]][["gene_n"]][[1]]), ",", fixed = TRUE)[[1]])
genes <- genes[genes != ""]
tmp_gmt <- tempfile(fileext = ".gmt")
writeLines(paste(c("BridgeTerm", "na", genes), collapse = "\t"), tmp_gmt)
if (requireNamespace("decoupleR", quietly = TRUE)) try(celltype_activity_decoupler(databases = character(0), custom_gmt_files = tmp_gmt, top = 1, qval = 1, min_term_genes = 1, export_excel = FALSE, plot_heatmap = FALSE), silent = TRUE)
}
}

