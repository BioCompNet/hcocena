% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_condition_significance.R
\name{module_condition_significance}
\alias{module_condition_significance}
\title{Module-Level Significance Between Conditions}
\usage{
module_condition_significance(
  set = "all",
  condition_col = NULL,
  donor_col = NULL,
  time_col = NULL,
  run_wilcox = TRUE,
  run_limma = TRUE,
  run_lmm = FALSE,
  lmm_include_time = TRUE,
  limma_reference = NULL,
  limma_trend = TRUE,
  padj = "BH",
  export_excel = TRUE,
  excel_file = "Module_condition_significance.xlsx",
  slot_name = "module_condition_significance"
)
}
\arguments{
\item{set}{Integer vector of layer indices or \code{"all"} (default).}

\item{condition_col}{Column name in annotation files defining conditions.
If \code{NULL}, uses \code{hcobject[["global_settings"]][["voi"]]}.}

\item{donor_col}{Optional donor identifier column in annotation files.
Required if \code{run_lmm = TRUE}.}

\item{time_col}{Optional time column in annotation files (used by LMM when
available and \code{lmm_include_time = TRUE}).}

\item{run_wilcox}{Logical; run Wilcoxon/Kruskal module-wise test.}

\item{run_limma}{Logical; run limma pairwise contrasts.}

\item{run_lmm}{Logical; run linear mixed model (\code{nlme::lme}) per module.}

\item{lmm_include_time}{Logical; include \code{time_col} as fixed effect in LMM
when available.}

\item{limma_reference}{Optional reference condition for one-vs-reference
limma contrasts. If \code{NULL}, all pairwise contrasts are tested.}

\item{limma_trend}{Logical; passed to \code{limma::eBayes(trend = ...)}.}

\item{padj}{Multiple-testing correction method (passed to \code{p.adjust}/limma).}

\item{export_excel}{Logical; write result tables to Excel in save folder.}

\item{excel_file}{File name for the Excel export.}

\item{slot_name}{Satellite slot name for storing results.}
}
\value{
Invisibly returns the stored result list.
}
\description{
Computes module-level statistics on sample-wise module means to identify
modules with significant differences between conditions.
}
\details{
Methods:
\itemize{
\item \code{Wilcoxon} (2 groups; for >2 groups an omnibus Kruskal-Wallis fallback is
used automatically),
\item \code{limma} pairwise contrasts,
\item \code{LMM} (\code{nlme::lme}) for repeated measures with donor random intercepts.
}

Results are stored in \code{hcobject[["satellite_outputs"]][[slot_name]]} and can
be added to the main module heatmap with
\code{plot_cluster_heatmap(include_module_significance = TRUE)}.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(module_condition_significance(set = "all", run_limma = FALSE, run_lmm = FALSE, export_excel = FALSE), silent = TRUE)
}
}

