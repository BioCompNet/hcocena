% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cytoscape.R
\name{export_to_local_folder}
\alias{export_to_local_folder}
\title{Export Integrated Network to local R session
Due to a lacking possibility of communication with Cytoscape from within Docker container, you need to export all necessary information for import into a local R session.}
\usage{
export_to_local_folder(
  file = base::paste0(hcobject[["working_directory"]][["dir_output"]],
    hcobject[["global_settings"]][["save_folder"]])
)
}
\arguments{
\item{file}{Path to the folder where the network information should be saved. Default path is set to the save folder defined in the global settings.}
}

\value{
Writes the current network and layout files to disk and returns invisibly.
}
\description{
Export Integrated Network to local R session
Due to a lacking possibility of communication with Cytoscape from within Docker container, you need to export all necessary information for import into a local R session.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(export_to_local_folder(file = tempdir()), silent = TRUE)
}
}

