% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cytoscape.R
\name{import_layout_from_local_folder}
\alias{import_layout_from_local_folder}
\title{Import network layout from file to your Docker container
Imports the layout generated within a local R session and Cytoscape back into the Docker container}
\usage{
import_layout_from_local_folder(
  file = base::paste0(hcobject[["working_directory"]][["dir_output"]],
    hcobject[["global_settings"]][["save_folder"]], "/network_layout.csv")
)
}
\arguments{
\item{file}{Exact path and file name containing the Cytoscape layout. Default path is set to the save folder defined in the global settings.}
}

\value{
Invisibly imports a saved layout into the legacy \code{hcobject}.
}
\description{
Import network layout from file to your Docker container
Imports the layout generated within a local R session and Cytoscape back into the Docker container
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
tmp_layout <- tempfile(fileext = ".csv")
write.csv(data.frame(row.names = integer(0)), tmp_layout)
try(import_layout_from_local_folder(file = tmp_layout), silent = TRUE)
}
}

