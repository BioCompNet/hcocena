% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_quick.R
\name{hc_longitudinal_step1_module_donor}
\alias{hc_longitudinal_step1_module_donor}
\title{Longitudinal step 1: module/donor clustering}
\usage{
hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "Subject",
  time_col = "Time_token",
  layer = NULL,
  time_levels,
  k = 2:6,
  rerolls = 1000,
  impute = TRUE,
  impute_method = "rfcont",
  ntree = 10,
  min_cluster_fraction = 0.1,
  score_method = "median",
  seed = 42,
  means_slot = "longitudinal_module_means",
  output_slot = "longitudinal_endotypes"
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{donor_col}{Annotation column containing donor IDs.}

\item{time_col}{Annotation column containing timepoint labels.}

\item{layer}{Optional layer index, layer id, or layer name. \code{NULL} uses the
first available layer.}

\item{time_levels}{Required explicit timepoint order vector.}

\item{k}{Candidate donor-trajectory cluster numbers per module. This is the
main parameter controlling how many donor clusters each module may form.}

\item{rerolls}{Number of repeated \code{kml} redrawings per module. Increase this
for more stable results.}

\item{impute}{Logical. If \code{TRUE}, impute missing donor-time values before
\code{kml} clustering.}

\item{impute_method}{Missing-value method passed to \code{mice}. Use \code{"rfcont"}
to match the previous workflow most closely. For \code{"rfcont"}, attach
\code{CALIBERrfimpute} first in the current session via
\code{library(CALIBERrfimpute)}.}

\item{ntree}{Number of trees for \code{rfcont} imputation.}

\item{min_cluster_fraction}{Minimum allowed fraction of donors in the
smallest cluster when scoring candidate \code{k}.}

\item{score_method}{Rule used to choose the final per-module clustering from
the \code{kml} score tables.}

\item{seed}{Random seed.}

\item{means_slot}{Satellite slot for module means.}

\item{output_slot}{Satellite slot for endotype outputs.}
}
\value{
A list with updated \code{hc}, step plots, and diagnostics.
}
\description{
Computes longitudinal module means, exact \code{kml} module/donor clustering, and
CAP, then returns the two wave plots and two heatmaps for this step.
}

\examples{
exists("hc_longitudinal_step1_module_donor", mode = "function")

\donttest{
TRUE
}
}

