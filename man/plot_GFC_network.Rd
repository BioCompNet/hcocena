% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gfc_network.R
\name{plot_GFC_network}
\alias{plot_GFC_network}
\title{Plot GFC Networks}
\usage{
plot_GFC_network()
}

\value{
Renders one GFC-coloured network plot per available comparison.
}
\description{
For visualization of the GFC for every gene under the different observed groups, the network can be additionally replotted once for every group,
with nodes being coloured according to their GFC value.
This provides a more detailed resolution of the information acquired from the module heatmap.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(plot_GFC_network(), silent = TRUE)
}
}

