% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_cutoff.R
\name{set_cutoff}
\alias{set_cutoff}
\title{Set Cutoff}
\usage{
set_cutoff(cutoff_vector = base::c())
}
\arguments{
\item{cutoff_vector}{A vector of cutoff values. The vector's length must be equal to the number of datasets. The order in which the cutoffs are set must correspond to the order in which the layers were declared in define_layers().}
}

\value{
Invisibly updates the cutoff vector in the legacy \code{hcobject}.
}
\description{
Set the correlation cutoffs for the different layers.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(set_cutoff(cutoff_vector = rep(0.9, length(hcobject[["layers"]]))), silent = TRUE)
}
}

