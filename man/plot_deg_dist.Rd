% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_deg_dist.R
\name{plot_deg_dist}
\alias{plot_deg_dist}
\title{Plot Degree Distribution}
\usage{
plot_deg_dist()
}

\value{
Renders degree-distribution plots for the current cutoff settings.
}
\description{
A function that plot the degree distribution of each network after being cut with the set cutoff values.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_after_part1.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(plot_deg_dist(), silent = TRUE)
}
}

