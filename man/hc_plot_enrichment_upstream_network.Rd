% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_plot_enrichment_upstream_network}
\alias{hc_plot_enrichment_upstream_network}
\title{Plot module knowledge network from enrichment + upstream inference (S4 API)}
\usage{
hc_plot_enrichment_upstream_network(
  hc,
  enrichment_mode = "selected",
  upstream_mode = "selected",
  clusters = c("all"),
  max_enrichment_per_module = NULL,
  max_upstream_per_module = NULL,
  label_mode = "both",
  show_plot = TRUE,
  save_pdf = TRUE,
  pdf_name = "Module_Knowledge_Network.pdf",
  gfc_scale_limits = NULL,
  heatmap_col_order = NULL,
  heatmap_cluster_columns = FALSE,
  pdf_width = NULL,
  pdf_height = NULL,
  pdf_pointsize = 11,
  overall_plot_scale = 1
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{enrichment_mode}{Character scalar. One of \code{"selected"} (default) or
\code{"significant"}.}

\item{upstream_mode}{Character scalar. One of \code{"selected"} (default) or
\code{"significant"}.}

\item{clusters}{Either \code{"all"} (default) or a character vector of module
colors to include.}

\item{max_enrichment_per_module}{Optional positive integer. If set, keeps at
most this many enrichment edges per module (best q-values first).}

\item{max_upstream_per_module}{Optional positive integer. If set, keeps at
most this many upstream edges per module (best q-values first).}

\item{label_mode}{Character scalar controlling term label density:
\code{"both"} (default), \code{"upstream_only"}, or \code{"focus_only"}.}

\item{show_plot}{Logical; if \code{TRUE} (default), prints the combined overview
(heatmap + network) in the active graphics device.}

\item{save_pdf}{Logical; if \code{TRUE} (default), writes a multi-page PDF to the
current hCoCena save folder (overview + per-module focus pages).}

\item{pdf_name}{Output PDF filename.}

\item{gfc_scale_limits}{Optional numeric vector controlling the left module
heatmap color scale limits. Provide one positive number (\code{x} -> \code{c(-x, x)})
or two numbers (\code{c(min, max)}). If NULL, uses upstream inference settings
first (if available), then main heatmap settings, then \code{c(-range_GFC, range_GFC)}.}

\item{heatmap_col_order}{Optional character vector overriding the hCoCena
heatmap column order for this knowledge-network plot only. If \code{NULL}
(default), the column order from the main module heatmap is reused when
available.}

\item{heatmap_cluster_columns}{Logical. If \code{FALSE} (default), reuse the
column order from the main hCoCena heatmap when available. If \code{TRUE},
cluster the columns for this knowledge-network plot instead.}

\item{pdf_width}{Optional numeric width (inches) for network PDFs.
If NULL (default), width is auto-estimated from content.}

\item{pdf_height}{Optional numeric height (inches) for network PDFs.
If NULL (default), height is auto-estimated from content.}

\item{pdf_pointsize}{Numeric base pointsize used for PDF export devices.
Default is 11.}

\item{overall_plot_scale}{Numeric scaling factor for text and line sizes.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Plot module knowledge network from enrichment + upstream inference (S4 API)
}

\examples{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
is(hc, "HCoCenaExperiment")

\donttest{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
hco <- as_hcobject(hc)
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hc <- as_hcocena(hco)
hco <- as_hcobject(hc)
genes <- trimws(strsplit(as.character(hco[["integrated_output"]][["cluster_calc"]][["cluster_information"]][["gene_n"]][[1]]), ",", fixed = TRUE)[[1]])
genes <- genes[genes != ""]
tmp_gmt <- tempfile(fileext = ".gmt")
writeLines(paste(c("BridgeTerm", "na", genes), collapse = "\t"), tmp_gmt)
try(hc <- hc_functional_enrichment(hc, gene_sets = character(0), custom_gmt_files = tmp_gmt, top = 1, pdf_width = 6, pdf_height = 6, pdf_pointsize = 8), silent = TRUE)
try(hc <- hc_upstream_inference(hc, resources = "TF", top = 1, qval = 1, plot = FALSE, save_pdf = FALSE, plot_per_comparison = FALSE), silent = TRUE)
try(hc <- hc_plot_enrichment_upstream_network(hc, show_plot = FALSE, save_pdf = FALSE), silent = TRUE)
}
}

