% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_modules.R
\name{unsplit_modules}
\alias{unsplit_modules}
\title{Undo module splitting}
\usage{
unsplit_modules(which = c("last", "all"), verbose = TRUE)
}
\arguments{
\item{which}{Either \code{"last"} (undo one split step) or \code{"all"} (restore the
original pre-split cluster state).}

\item{verbose}{Logical; print progress messages.}
}

\value{
Invisibly restores the unsplit-module state in the legacy \code{hcobject}.
}
\description{
Restores cluster assignments from the split history created by
\code{\link[=split_modules]{split_modules()}}.
}

\examples{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
is.list(hco)

\donttest{
hco <- readRDS(system.file("extdata", "hcobject_clustered.rds", package = "hcocena"))
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hcocena:::.hc_assign_legacy_hcobject(hco)
assign("hcobject", hco, envir = .GlobalEnv)
try(unsplit_modules(), silent = TRUE)
}
}

