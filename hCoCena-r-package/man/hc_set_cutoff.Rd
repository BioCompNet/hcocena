% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_set_cutoff}
\alias{hc_set_cutoff}
\title{Set network cutoffs (S4 API)}
\usage{
hc_set_cutoff(
  hc,
  cutoff_vector = base::c(),
  auto = FALSE,
  fallback_cutoff = 0.982,
  verbose = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{cutoff_vector}{A vector of cutoff values. The vector's length must be equal to the number of datasets. The order in which the cutoffs are set must correspond to the order in which the layers were declared in define_layers().}

\item{auto}{Logical; if \code{TRUE}, automatically select cutoffs from available
tuning outputs in this priority:
\enumerate{
\item \code{hc@satellite$cutoff_tuning$applied_cutoff_vector},
\item \code{hc@satellite$cutoff_tuning$recommended_cutoff_vector},
\item \code{hc@satellite$auto_tune$cutoff$recommended_cutoff_vector},
\item internal simple auto cutoff extraction from layer results,
\item existing \code{hc@config@layer$cutoff},
\item user-provided \code{cutoff_vector},
\item \code{fallback_cutoff}.
}}

\item{fallback_cutoff}{Numeric fallback cutoff used for still-missing layers
(for both \code{auto = TRUE} and \code{auto = FALSE}).}

\item{verbose}{Logical; print additional source details.
The final applied cutoff vector is always printed.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Set network cutoffs (S4 API)
}
