% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_read_data}
\alias{hc_read_data}
\title{Read expression and annotation data (S4 API)}
\usage{
hc_read_data(
  hc,
  sep_counts = "\\t",
  sep_anno = "\\t",
  gene_symbol_col = NULL,
  sample_col = NULL,
  count_has_rn = TRUE,
  anno_has_rn = TRUE,
  auto_setup_output = TRUE,
  create_output_dir = TRUE,
  project_folder = NULL
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{sep_counts}{The separator of the count files. Default is tab separated files. Ignore when loading data from objects instead of files.}

\item{sep_anno}{The separator of the annotation files. Default is tab separated files. Ignore when loading data from objects instead of files.}

\item{gene_symbol_col}{A String. Name of the column that contains the gene symbols. Ignore when loading data from objects instead of files.}

\item{sample_col}{A String. Name of the column that contains the sample IDs. Ignore when loading data from objects instead of files.}

\item{count_has_rn}{A Boolean. Whether or not the count file has rownames. Default is TRUE. Ignore when loading data from objects instead of files.}

\item{anno_has_rn}{A Boolean. Whether or not the annotation file has rownames. Default is TRUE. Ignore when loading data from objects instead of files.}

\item{auto_setup_output}{Boolean. If \code{TRUE}, run \code{hc_check_dirs()} and
initialize the save folder before reading data (if \code{dir_output} is set).}

\item{create_output_dir}{Boolean passed to \code{hc_check_dirs()} when
\code{auto_setup_output = TRUE}.}

\item{project_folder}{Optional save subfolder name. Use \code{""} to write
directly into \code{dir_output}. If \code{NULL}, keep an already configured
\code{save_folder} or default to \code{""}.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Read expression and annotation data (S4 API)
}
\examples{
extdir <- paste0(
  normalizePath(system.file("extdata", package = "hcocena"), winslash = "/"),
  "/"
)
outdir <- file.path(tempdir(), "hcocena-read-data")
dir.create(outdir, recursive = TRUE, showWarnings = FALSE)
hc <- hc_init()
hc <- hc_set_paths(
  hc,
  dir_count_data = extdir,
  dir_annotation = extdir,
  dir_reference_files = extdir,
  dir_output = outdir
)
hc <- hc_define_layers(
  hc,
  data_sets = list(
    Layer1 = c("toy_layer1_counts.tsv", "toy_layer1_anno.tsv"),
    Layer2 = c("toy_layer2_counts.tsv", "toy_layer2_anno.tsv")
  )
)
hc <- hc_read_data(
  hc,
  sep_counts = "\t",
  sep_anno = "\t",
  gene_symbol_col = "SYMBOL",
  sample_col = "SampleID",
  count_has_rn = FALSE,
  anno_has_rn = FALSE
)
names(MultiAssayExperiment::experiments(hc_mae(hc)))
}
