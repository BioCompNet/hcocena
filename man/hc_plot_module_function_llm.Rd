% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_plot_module_function_gemini.R
\name{hc_plot_module_function_llm}
\alias{hc_plot_module_function_llm}
\alias{hc_plot_module_function_gemini}
\title{Plot AI-assisted module function summaries}
\usage{
hc_plot_module_function_llm(
  hc,
  slot_name = "llm_module_function",
  modules = NULL,
  fields = c("general_processes", "contextual_state", "key_regulators"),
  max_chars = 90,
  text_size = 4,
  with_heatmap = TRUE,
  heatmap_col_order = NULL,
  heatmap_cluster_columns = FALSE,
  heatmap_rel_width = 1.7,
  text_rel_width = 1.25,
  title = "AI-assisted module interpretation"
)

hc_plot_module_function_gemini(...)
}
\arguments{
\item{hc}{An \code{HCoCenaExperiment} with stored results from
\code{hc_module_function_llm()}.}

\item{slot_name}{Satellite slot name used for storage. Default is
\code{"llm_module_function"}.}

\item{modules}{Optional character vector to subset modules.}

\item{fields}{Character vector selecting which LLM fields to plot. Supported
values are \code{"general_processes"}, \code{"contextual_state"}, and
\code{"key_regulators"}. Defaults to all three.}

\item{max_chars}{Maximum number of characters shown per term. Default is
\code{90}.}

\item{text_size}{Numeric text size passed to \code{ggplot2::geom_text()}.}

\item{with_heatmap}{Logical. If \code{TRUE}, reuse the stored hCoCena cluster
heatmap and place the LLM interpretations as a right-side annotation.
Falls back to a text-only plot if no heatmap cache is available.}

\item{heatmap_col_order}{Optional character vector overriding the hCoCena
heatmap column order for this LLM plot only. If \code{NULL} (default), the
column order from the main module heatmap is reused when available.}

\item{heatmap_cluster_columns}{Logical. If \code{FALSE} (default), reuse the
column order from the main hCoCena heatmap when available. If \code{TRUE},
cluster the columns for this LLM plot instead.}

\item{heatmap_rel_width}{Relative width of the heatmap panel when
\code{with_heatmap = TRUE}.}

\item{text_rel_width}{Relative width of the text panel when
\code{with_heatmap = TRUE}.}

\item{title}{Plot title. Default is \code{"AI-assisted module interpretation"}.}

\item{...}{Used by the backward-compatible wrapper alias
\code{hc_plot_module_function_gemini()}.}
}
\value{
A single plot object when one field is selected, otherwise a named
list of plot objects.
}
\description{
Draws a compact overview of stored AI-assisted module function summaries with
a colored module box on the left and the inferred overarching function on the
right.
}
\examples{
exists("hc_plot_module_function_llm", mode = "function")

\donttest{
p <- hc_plot_module_function_llm(hc)
print(p)
}
}
