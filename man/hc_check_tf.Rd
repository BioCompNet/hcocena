% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_check_tf}
\alias{hc_check_tf}
\title{Check TF targets (S4 API)}
\usage{
hc_check_tf(hc, TF)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{TF}{Transcription factor symbol.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Check TF targets (S4 API)
}

\examples{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
is(hc, "HCoCenaExperiment")

\donttest{
hc <- readRDS(system.file("extdata", "hc_clustered.rds", package = "hcocena"))
hco <- as_hcobject(hc)
hco[["working_directory"]][["dir_output"]] <- paste0(normalizePath(tempdir(), winslash = "/", mustWork = FALSE), "/")
hco[["global_settings"]][["save_folder"]] <- ""
hc <- as_hcocena(hco)
try(hc <- hc_check_tf(hc, TF = "STAT1"), silent = TRUE)
}
}

